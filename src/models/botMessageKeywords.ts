export enum BotMessageKeywords {
  chatDocument = "/chatDocument",
  chatGPT = "/chatGPT",
  document = "/document",
  debug = "/debug",
  forget = "/forget",
  history = "/history",
  reset = "/reset",
  welcome = "/welcome",
  me = "/me",
}
